# g4f fallback

يستخدم الخادم **g4f** كمسار احتياطي بلا مفتاح API عندما تستنفد مزودات الذكاء الاصطناعي المهيأة أو لا توجد مفاتيح صالحة. يستمر استخدام المزودات الحالية أولًا، لذلك لا يتغير السلوك عند توفر مفاتيح OpenRouter أو OpenAI أو غيرها.

## التثبيت المحلي

```bash
python3 -m pip install -r requirements-g4f.txt
```

يحتاج الخادم إلى Python 3 وإلى وجود الملف `artifacts/api-server/src/lib/g4f-bridge.py`. يمكن تغيير مسار Python عبر `PYTHON_BIN`، ومسار الجسر عبر `G4F_BRIDGE_PATH`.

## الإعدادات الاختيارية

| المتغير | القيمة الافتراضية | الغرض |
|---|---:|---|
| `G4F_MODEL` | `gpt-4o-mini` | النموذج الذي يمرره g4f |
| `G4F_TIMEOUT_MS` | `90000` | الحد الأقصى لانتظار الاستجابة |
| `PYTHON_BIN` | `python3` | أمر تشغيل Python |
| `G4F_BRIDGE_PATH` | مسار الجسر داخل المشروع | مسار ملف الجسر |

في Railway/Nixpacks، يضيف `nixpacks.toml` Python 3.11 ويثبت `requirements-g4f.txt` تلقائيًا.

> g4f مشروع مجتمعي يعتمد على مزودات مجانية قد تتغير أو تتوقف أو تفرض حدودًا. لذلك يبقى fallback احتياطيًا وليس بديلًا مضمونًا لمزود رسمي.
